<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://Nixuebing.github.io/blog</id>
    <title>冰叶小屋</title>
    <updated>2019-11-25T08:30:07.513Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://Nixuebing.github.io/blog"/>
    <link rel="self" href="https://Nixuebing.github.io/blog/atom.xml"/>
    <subtitle>温故而知新</subtitle>
    <logo>https://Nixuebing.github.io/blog/images/avatar.png</logo>
    <icon>https://Nixuebing.github.io/blog/favicon.ico</icon>
    <rights>All rights reserved 2019, 冰叶小屋</rights>
    <entry>
        <title type="html"><![CDATA[Spring IoC]]></title>
        <id>https://Nixuebing.github.io/blog/post/spring-ioc</id>
        <link href="https://Nixuebing.github.io/blog/post/spring-ioc">
        </link>
        <updated>2019-07-04T07:28:57.000Z</updated>
        <summary type="html"><![CDATA[<p>容器创建 Bean 对象，将它们装配在一起，配置它们并管理它们的完整生命周期。</p>
]]></summary>
        <content type="html"><![CDATA[<p>容器创建 Bean 对象，将它们装配在一起，配置它们并管理它们的完整生命周期。</p>
<!-- more -->
<p>· Spring 容器使用依赖注入来管理组成应用程序的 Bean 对象。<br>
· 容器通过读取提供的配置元数据 Bean Definition 来接收对象进行实例化，配置和组装的指令。<br>
· 该配置元数据 Bean Definition 可以通过 XML，Java 注解或 Java Config 代码提供。</p>
<figure data-type="image" tabindex="1"><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20191125/YuhoqTlfoWNG.png?imageslim" alt="mark"></figure>
<h2 id="可以通过三种方式完成依赖注入">可以通过三种方式完成依赖注入？</h2>
<p>· 接口注入<br>
· 构造函数注入<br>
· setter 注入<br>
目前，在 Spring Framework 中，仅使用构造函数和 setter 注入这两种方式。</p>
<h2 id="spring-中有两种-ioc-容器">Spring 中有两种 IoC 容器？</h2>
<p>BeanFactory ，就像一个包含 Bean 集合的工厂类。它会在客户端要求时实例化 Bean 对象。</p>
<p>ApplicationContext 接口扩展了 BeanFactory 接口，它在 BeanFactory 基础上提供了一些额外的功能。内置如下功能：</p>
<p>BeanFactory 也被称为低级容器，而 ApplicationContext 被称为高级容器。<br>
<img src="http://pic-cloud.ice-leaf.top/pic-cloud/20191125/kFom1EXzrQWX.png?imageslim" alt="mark"></p>
<h2 id="spring-bean-在容器的生命周期">Spring Bean 在容器的生命周期</h2>
<p>· 实例化 Bean 对象<br>
Spring 容器根据配置中的 Bean Definition(通过 XML，Java 注解或 Java Config 代码提供)中实例化 Bean 对象。<br>
Spring 使用依赖注入填充所有属性，如 Bean 中所定义的配置。</p>
<p>· Aware 相关的属性，注入到 Bean 对象<br>
· 调用相应的方法，进一步初始化 Bean 对象<br>
· 如果 Bean 实现 DisposableBean 接口，当 spring 容器关闭时，会调用 #destroy() 方法。<br>
· 如果为 bean 指定了 destroy 方法（例如 <bean /> 的 destroy-method 属性），那么将调用该方法。</p>
<figure data-type="image" tabindex="2"><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20191125/F4hqfQoPRduo.png?imageslim" alt="mark"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring 框架中的设计模式]]></title>
        <id>https://Nixuebing.github.io/blog/post/spring-kuang-jia-zhong-de-she-ji-mo-shi</id>
        <link href="https://Nixuebing.github.io/blog/post/spring-kuang-jia-zhong-de-she-ji-mo-shi">
        </link>
        <updated>2019-07-01T02:45:34.000Z</updated>
        <content type="html"><![CDATA[<h1 id="解释器模式">解释器模式</h1>
<p>在现实世界中，我们人类需要解释手势。他们可以对文化有不同的含义。这是我们的解释，给他们一个意义。在编程中，我们还需要分析一件事情，并决定它是什么意思。我们可以用解释设计模式来做。</p>
<p>Spring Expression Language（Spel）</p>
<h1 id="创建模式三剑客">创建模式三剑客</h1>
<p>· 建设者模式<br>
· 工厂模式<br>
· 抽象工厂模式</p>
<h1 id="代理模式">代理模式</h1>
<p>面向切面编程（AOP）</p>
<h1 id="组合模式">组合模式</h1>
<p>这种模式是基于具有共同行为的多个对象的存在，用于构建更大的对象。较大的对象仍然具有与最小对象相同的特征。如多个监听器，放入一个 composite.list 循环执行</p>
<h1 id="策略模式">策略模式</h1>
<p>策略定义了通过不同方式完成相同事情的几个对象。完成任务的方式取决于采用的策略。例如在 web 中的策略运用，方法可以通过多个条件解决选择不同策略：属性映射，HTTP请求参数或URL路径。</p>
<h1 id="模板模式">模板模式</h1>
<p>此模式定义了类行为的骨架，并将子步骤的某些步骤的延迟执行(具体就是一个方法放在另一个方法中，只有调用另一方方法的时候这个方法才会执行,而且还可能会在其他行为方法之后按顺序执行)。</p>
<h1 id="观察者模式">观察者模式</h1>
<h1 id="适配器模式">适配器模式</h1>
<h1 id="装饰器模式">装饰器模式</h1>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[并发工具类]]></title>
        <id>https://Nixuebing.github.io/blog/post/bing-fa-gong-ju-lei</id>
        <link href="https://Nixuebing.github.io/blog/post/bing-fa-gong-ju-lei">
        </link>
        <updated>2019-06-18T02:40:24.000Z</updated>
        <summary type="html"><![CDATA[<p><strong>Java SDK 并发包通过 Lock 和 Condition 两个接口来实现管程，其中 Lock 用于解决互斥问题，Condition 用于解决同步问题。</strong></p>
]]></summary>
        <content type="html"><![CDATA[<p><strong>Java SDK 并发包通过 Lock 和 Condition 两个接口来实现管程，其中 Lock 用于解决互斥问题，Condition 用于解决同步问题。</strong></p>
<!-- more -->
<p>用锁的最佳实践：</p>
<ol>
<li>永远只在更新对象的成员变量时加锁</li>
<li>永远只在访问可变的成员变量时加锁</li>
<li>永远不在调用其他对象的方法时加锁</li>
</ol>
<h2 id="lock">Lock</h2>
<p>Lock 有别于 synchronized 隐式锁的三个特性：</p>
<ul>
<li>能够响应中断</li>
<li>支持超时 - 如果线程在一段时间之内没有获取到锁，不是进入阻塞状态，而是返回一个错误</li>
<li>非阻塞地获取锁 - 如果尝试获取锁失败，并不进入阻塞状态，而是直接返回</li>
</ul>
<pre><code class="language-java">// 支持中断的 API
void lockInterruptibly() throws InterruptedException;
// 支持超时的 API
boolean tryLock(long time, TimeUnit unit) throws InterruptedException;
// 支持非阻塞获取锁的 API
boolean tryLock();
</code></pre>
<h2 id="condition">Condition</h2>
<p>线程等待和通知需要调用 await()、asignal()、asignalAll()<br>
参考 Dubbo 源码 DefaultFuture.get</p>
<h2 id="readwritelock">ReadWriteLock</h2>
<p>针对读多写少的并发场景</p>
<ol>
<li>允许多个线程同时读共享变量；</li>
<li>只允许一个线程写共享变量；</li>
<li>如果一个写线程正在执行写操作，此时禁止读线程读取共享变量。</li>
</ol>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java线程的生命周期]]></title>
        <id>https://Nixuebing.github.io/blog/post/java-xian-cheng-de-sheng-ming-zhou-qi</id>
        <link href="https://Nixuebing.github.io/blog/post/java-xian-cheng-de-sheng-ming-zhou-qi">
        </link>
        <updated>2019-06-17T08:05:55.000Z</updated>
        <summary type="html"><![CDATA[<p>Java语言中线程共用六种状态，分别是：</p>
<ol>
<li>NEW（初始化状态）</li>
<li>RUNNABLE （可运行/运行状态）</li>
<li>BLOCKED （阻塞状态）</li>
<li>WAITING （无时限等待）</li>
<li>TIMED_WAITING （有时限等待）</li>
<li>TERMINATED （终止状态）</li>
</ol>
]]></summary>
        <content type="html"><![CDATA[<p>Java语言中线程共用六种状态，分别是：</p>
<ol>
<li>NEW（初始化状态）</li>
<li>RUNNABLE （可运行/运行状态）</li>
<li>BLOCKED （阻塞状态）</li>
<li>WAITING （无时限等待）</li>
<li>TIMED_WAITING （有时限等待）</li>
<li>TERMINATED （终止状态）</li>
</ol>
<!-- more -->
<figure data-type="image" tabindex="1"><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20190617/LJLS86afRhQp.png?imageslim" alt="mark"></figure>
<h2 id="1-runnable-与-blocked-的状态转换">1、RUNNABLE 与 BLOCKED 的状态转换</h2>
<p>只有一种情况会触发，就是线程等待和获取 synchronized 的隐式锁。</p>
<h2 id="2-runnable-与-waiting-的状态转换">2、RUNNABLE 与 WAITING 的状态转换</h2>
<ol>
<li>获得 synchronized 隐式锁的线程，调用无参数的 Object.wait()</li>
<li>调用无参数的 Thread.join() 方法，等待中的线程其状态会从 RUNNABLE 转换到 WAITING</li>
<li>调用 LockSupport.park()</li>
</ol>
<h2 id="3-runnable-与-timed_waiting-的状态转换">3、RUNNABLE 与 TIMED_WAITING 的状态转换</h2>
<p>五种场景方法：<br>
Thread.sleep(long millis)<br>
Object.wait(long timeout)<br>
Thread.join(long millis)<br>
LockSupport.parkNanos(Object blocker, long deadline)<br>
LockSupport.parkUntil(long deadline)</p>
<h2 id="4-从-new-到-runnable">4、从 NEW 到 RUNNABLE</h2>
<p>通过 Thread.start() 实现</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Java并发编程]]></title>
        <id>https://Nixuebing.github.io/blog/post/java-bing-fa-bian-cheng</id>
        <link href="https://Nixuebing.github.io/blog/post/java-bing-fa-bian-cheng">
        </link>
        <updated>2019-06-13T07:25:36.000Z</updated>
        <summary type="html"><![CDATA[<p>核心矛盾在于CPU、内存、I/O设备的速度差异。</p>
]]></summary>
        <content type="html"><![CDATA[<p>核心矛盾在于CPU、内存、I/O设备的速度差异。</p>
<!-- more -->
<p>为了合理利用CPU的高性能，平衡三者的速度差异：</p>
<ol>
<li>CPU 增加了缓存，以均衡与内存的速度差异；</li>
<li>操作系统增加了进程、线程，以分时复用 CPU，进而均衡 CPU 与 I/O 设备的速度差异；</li>
<li>编译程序优化指令执行次序，使得缓存能够得到更加合理地利用。</li>
</ol>
<h1 id="并发bug的源头">并发Bug的源头</h1>
<h2 id="源头之一缓存导致的可见性问题">源头之一：缓存导致的可见性问题</h2>
<p>一个线程对共享变量的修改，另外一个线程能够立刻看到，我们称为<strong>可见性</strong>。</p>
<figure data-type="image" tabindex="1"><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20190613/26wHwGbSupfO.png?imageslim" alt="mark"></figure>
<p>线程A对变量V的操作对于线程B而言就不具备可见性。</p>
<h2 id="源头之二线程切换带来的原子性问题">源头之二：线程切换带来的原子性问题</h2>
<p>我们把一个或多个操作在CPU执行的过程中不被中断的特性称为<strong>原子性</strong>。</p>
<p>高级语言里的一条语句往往需要多条CPU指令完成，例如 <code>count += 1</code>，至少需要三条CPU指令。</p>
<ul>
<li>指令 1：首先，需要把变量 count 从内存加载到 CPU 的寄存器；</li>
<li>指令 2：之后，在寄存器中执行 +1 操作；</li>
<li>指令 3：最后，将结果写入内存（缓存机制导致可能写入的是 CPU 缓存而不是内存）。<br>
而操作系统做任务切换，可以发生在任何一条<strong>CPU指令</strong>执行完，而不是高级语言里的一条语句。</li>
</ul>
<figure data-type="image" tabindex="2"><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20190613/twvANvmYc0FL.png?imageslim" alt="mark"></figure>
<h2 id="源头之三编译优化带来的有序性问题">源头之三：编译优化带来的有序性问题</h2>
<p>双重检查创建单例对象案例：</p>
<pre><code class="language-java">public class Singleton {
  static Singleton instance;
  static Singleton getInstance(){
    if (instance == null) {
      synchronized(Singleton.class) {
        if (instance == null)
          instance = new Singleton();
        }
    }
    return instance;
  }
}
</code></pre>
<p>假设有两个线程 A、B 同时调用 getInstance() 方法，他们会同时发现 <code>instance == null</code> ，于是同时对 Singleton.class 加锁，此时 JVM 保证只有一个线程能够加锁成功（假设是线程 A），另外一个线程则会处于等待状态（假设是线程 B）；线程 A 会创建一个 Singleton 实例，之后释放锁，锁释放后，线程 B 被唤醒，线程 B 再次尝试加锁，此时是可以加锁成功的，加锁成功后，线程 B 检查 <code>instance == null</code>  时会发现，已经创建过 Singleton 实例了，所以线程 B 不会再创建一个 Singleton 实例。</p>
<p>我们以为的new操作应该是：</p>
<ol>
<li>分配一块内存 M；</li>
<li>在内存 M 上初始化 Singleton 对象；</li>
<li>然后 M 的地址赋值给 instance 变量。</li>
</ol>
<p>但是实际上优化后的执行路径却是这样的：</p>
<ol>
<li>分配一块内存 M；</li>
<li>将 M 的地址赋值给 instance 变量；</li>
<li>最后在内存 M 上初始化 Singleton 对象。</li>
</ol>
<figure data-type="image" tabindex="3"><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20190613/ygdirenM1hMT.png?imageslim" alt="mark"></figure>
<h1 id="解决可见性和有序性问题">解决可见性和有序性问题</h1>
<p>导致可见性的原因是缓存，导致有序性的原因是编译优化，所以最直接的办法就是<strong>禁用缓存和编译优化</strong>（程序性能就堪忧了）。<br>
合理的方案是<strong>按需禁用缓存以及编译优化</strong>。</p>
<h2 id="volatile">volatile</h2>
<p>最原始的意义就是禁用CPU缓存。</p>
<h2 id="happens-before-规则">Happens-Before 规则</h2>
<p><strong>前面一个操作的结果对后续操作是可见的。</strong></p>
<ol>
<li>在一个线程中，按照程序顺序，前面的操作 Happens-Before 于后续的任意操作。</li>
<li>对于一个 volatile 变量的写操作，Happens-Before 于后续对这个 volatile 变量的读操作。</li>
<li>如果 A Happens-Before B，且 B Happens-Before C，那么 A Happens-Before C。</li>
</ol>
<p><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20190613/ml8RGWltuC0r.png?imageslim" alt="mark"><br>
“x=42” Happens-Before 写变量 “v=true” ，这是规则 1 的内容；<br>
写变量“v=true” Happens-Before 读变量 “v=true”，这是规则 2 的内容 。<br>
根据规则 3 的内容，如果线程 B 读到了“v=true”，那么线程 A 设置的“x=42”对线程 B 是可见的。</p>
<ol start="4">
<li>对一个锁的解锁 Happens-Before 于后续对这个锁的加锁。</li>
<li>主线程 A 启动子线程 B 后，子线程 B 能够看到主线程在启动子线程 B 前的操作。或者说，如果线程 A 调用线程 B 的 start() 方法（即在线程 A 中启动线程 B），那么该 start() 操作 Happens-Before 于线程 B 中的任意操作。</li>
<li>主线程 A 等待子线程 B 完成（主线程 A 通过调用子线程 B 的 join() 方法实现），当子线程 B 完成后（主线程 A 中 join() 方法返回），主线程能够看到子线程的操作。当然所谓的“看到”，指的是对<strong>共享变量</strong>的操作。或者说，如果在线程 A 中，调用线程 B 的 join() 并成功返回，那么线程 B 中的任意操作 Happens-Before 于该 join() 操作的返回。</li>
</ol>
<h2 id="final">final</h2>
<p>final 修饰变量时，初衷是告诉编译器：这个变量生而不变，可以可劲儿优化。</p>
<h1 id="解决原子性问题">解决原子性问题</h1>
<p>原子性问题的源头是<strong>线程切换</strong>。</p>
<h2 id="synchronize">synchronize</h2>
<blockquote>
<p>当修饰静态方法时，锁定的是当前类的 Class 对象；<br>
当修饰非静态方法的时候，锁定的是当前实例对象 this。</p>
</blockquote>
<pre><code class="language-java">class X {
  // 修饰静态方法
  synchronized(X.class) static void bar() {
    // 临界区
  }
}
</code></pre>
<pre><code class="language-java">class X {
  // 修饰非静态方法
  synchronized(this) void foo() {
    // 临界区
  }
}
</code></pre>
<p><strong>锁和受保护资源的关系：受保护资源和锁之间的关联关系是 N:1 的关系。</strong></p>
<pre><code class="language-java">class SafeCalc {
  static long value = 0L;
  synchronized long get() {
    return value;
  }
  synchronized static void addOne() {
    value += 1;
  }
}
</code></pre>
<figure data-type="image" tabindex="4"><img src="http://pic-cloud.ice-leaf.top/pic-cloud/20190617/HEicy1QE06wD.png?imageslim" alt="mark"></figure>
<pre><code class="language-java">class Account {
  private int balance;
  // 转账
  synchronized void transfer(
      Account target, int amt){
    if (this.balance &gt; amt) {
      this.balance -= amt;
      target.balance += amt;
    }
  } 
}
</code></pre>
<p>看似正确，但是 this 这把锁可以保护自己的余额 this.balance，却保护不了别人的余额 target.balance。</p>
<h2 id="死锁">死锁</h2>
<p><strong>细粒度锁：用不同的锁对受保护资源进行精细化管理，能够提升性能。</strong><br>
但是使用细粒度锁是有代价的，这个代价就是可能会导致死锁。</p>
<p><strong>死锁：一组互相竞争资源的线程因互相等待，导致“永久”阻塞的现象。</strong><br>
解决死锁最好的办法还是规避死锁。</p>
<p>只有以下四个条件都发生时才会出现死锁：</p>
<ol>
<li>互斥，共享资源 X 和 Y 只能被一个线程占用；</li>
<li>占用且等待，线程 T1 已经取得共享资源 X，在等待共享资源 Y 的时候，不释放共享资源 X；</li>
<li>不可抢占，其他线程不能强行抢占线程 T1 占用的资源；</li>
<li>循环等待，线程 T1 等待线程 T2 占用的资源，线程 T2 等待线程 T1 占用的资源，就是循环等待。</li>
</ol>
<p><strong>只要破坏其中一个，就可以成功避免死锁的发生。</strong></p>
<p>互斥这个条件我们没法破坏，因为我们用锁为的就是互斥。</p>
<h3 id="1破坏占用且等待条件">1.破坏占用且等待条件</h3>
<p>理论上要破坏这个条件，可以一次性申请所有资源。<br>
<strong>等待-通知机制：</strong> 如果线程要求的条件不满足，则线程阻塞自己，进入等待状态；当线程要求的条件满足后，通知等待的线程重新执行。<br>
wait(), notify(), notifyAll()  方法操作的等待队列是互斥锁的等待队列，所以如果 synchronized 锁定的是 this，那么对应的一定是 this.wait(), this.notify(), this.notifyAll()。<br>
<strong>尽量使用 notifyAll()，notify() 是会随机地通知等待队列中的一个线程（风险在于可能导致某些线程永远不会被通知到），而 notifyAll() 会通知等待队列中的所有线程。</strong></p>
<h3 id="2破坏不可抢占条件">2.破坏不可抢占条件</h3>
<p>核心就是要能够主动释放它占有的资源。这一点 synchronized 是做不到的。原因是 synchronized 申请资源的时候，如果申请不到，线程直接进入阻塞状态了，而线程进入阻塞状态，啥都干不了，也释放不了线程已经占有的资源。</p>
<h3 id="3破坏循环等待条件">3.破坏循环等待条件</h3>
<p>破坏这个条件，需要对资源进行排序，然后按序申请资源。</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[可转债]]></title>
        <id>https://Nixuebing.github.io/blog/post/ke-zhuan-zhai</id>
        <link href="https://Nixuebing.github.io/blog/post/ke-zhuan-zhai">
        </link>
        <updated>2019-05-31T01:44:29.000Z</updated>
        <summary type="html"><![CDATA[<p>可转债：是可以转换成公司股票的债券，由上市公司发行。</p>
]]></summary>
        <content type="html"><![CDATA[<p>可转债：是可以转换成公司股票的债券，由上市公司发行。</p>
<!-- more -->
<h2 id="下有保底">下有保底</h2>
<p>当公司股票在任意连续三十个交易日中至少有二十个交易日收盘价低于当期转股价格的80%时开股东大会，商议是否下调转股价。转股价下调，对应的可转债票面价值就会提升。</p>
<p><strong>原来：</strong><br>
<strong>100元可转债=52.5元的股票价格</strong><br>
<strong>下调转股价后</strong><br>
<strong>100元可转债=40元的股票价格</strong></p>
<p>如果公司股票在任何连续三十个交易日的收盘价格低于当期转股价格的70%时，公司以约定的价格回售。</p>
<h2 id="上不封顶">上不封顶</h2>
<p>到130时可以转股</p>
<p><strong>强制赎回</strong>：如果公司A股股票连续三十个交易日中至少有十五个交易日的收盘价格不低于当期转股价格的130%（含130%），这个时候你就要转股了，不转股上市公司要以回售价格（103）收回了。</p>
<h2 id="挑选方法">挑选方法</h2>
<p><a href="https://www.jisilu.cn/data/cbnew/#cb">集思录</a></p>
<p>溢价率&lt;20%<br>
到期收益率&gt;0%<br>
评级是AA级以上（也就是AA,AA+,AAA是符合的）<br>
上市满半年</p>
<h2 id="卖出方法">卖出方法</h2>
<p>当可转债涨到130块以上时，只要从最高点跌去10块钱，就可以卖出。</p>
<table>
<thead>
<tr>
<th>可转债名称</th>
<th>当前价格</th>
<th>溢价率</th>
<th>到期收益率</th>
</tr>
</thead>
<tbody>
<tr>
<td>国贸转债</td>
<td>110.880</td>
<td>13.77%</td>
<td>0.18%</td>
</tr>
<tr>
<td>光大转债</td>
<td>108.050</td>
<td>15.01%</td>
<td>0.31%</td>
</tr>
<tr>
<td>湖广转债</td>
<td>108.610</td>
<td>5.16%</td>
<td>0.91%</td>
</tr>
<tr>
<td>山鹰转债</td>
<td>106.900</td>
<td>5.32%</td>
<td>1.94%</td>
</tr>
<tr>
<td>江银转债</td>
<td>105.189</td>
<td>5.64%</td>
<td>0.90%</td>
</tr>
<tr>
<td>张行转债</td>
<td>102.803</td>
<td>10.07%</td>
<td>2.00%</td>
</tr>
<tr>
<td>苏农转债</td>
<td>105.950</td>
<td>10.66%</td>
<td>1.74%</td>
</tr>
<tr>
<td>无锡转债</td>
<td>101.290</td>
<td>17.93%</td>
<td>1.77%</td>
</tr>
</tbody>
</table>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[股市入场判断标准]]></title>
        <id>https://Nixuebing.github.io/blog/post/gu-shi-ru-chang-pan-duan-biao-zhun</id>
        <link href="https://Nixuebing.github.io/blog/post/gu-shi-ru-chang-pan-duan-biao-zhun">
        </link>
        <updated>2019-05-28T07:23:57.000Z</updated>
        <summary type="html"><![CDATA[<p><strong>当沪深300的“股息率”，接近甚至大于3年期国债收益率的话，那么股票就具有极高的投资价值。</strong></p>
]]></summary>
        <content type="html"><![CDATA[<p><strong>当沪深300的“股息率”，接近甚至大于3年期国债收益率的话，那么股票就具有极高的投资价值。</strong></p>
<!--more-->
<p><code>股票吸引力指数 = 沪深300的股息率 / 3年期国债收益率。</code></p>
<p><strong>“股票吸引力指数”如果接近1，甚至大于1，那就代表股票非常适合投资。</strong></p>
<p>沪深300股息率：<a href="http://www.csindex.com.cn/zh-CN/downloads/index-information">指数估值</a><br>
3年前国债收益率： <a href="https://cn.investing.com/rates-bonds/china-3-year-bond-yield">英为财情</a></p>
]]></content>
    </entry>
</feed>